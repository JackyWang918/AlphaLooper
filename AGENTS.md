# AlphaLooper 协作约定

继续本项目之前，先阅读：

1. `docs/project-context.md`：用户目的、已确定需求、当前进度、待确认策略与换机交接。
2. `README.md`：环境安装、运行和验证方式。
3. `docs/technical-plan.md`：技术架构与阶段验收标准。
4. 涉及策略时阅读 `docs/strategy.md`：最新已确认参数与待确认细节。

不要要求用户重新描述以上文档中已明确记录的背景。文档里的“建议”“待确认”不代表用户已经同意；继续讨论这些具体问题即可。

## 实施约定

- 单仓库，前端在 `frontend`，后端在 `backend`。Python 用 uv，前端用 npm；提交对应锁文件。
- 本地源码运行；目前不引入服务器部署、Docker 或安装包需求。
- 登录、验证码及平台提示由用户手动处理，不绕过验证或平台限制。
- 用户已授权开发程序的实盘单笔提交功能，由用户在本地开启运行。仅填表入口保持不提交；开发自动化测试使用隔离页面，不在真实账户执行买卖。
- 未登录页面测试、模拟页面测试、登录后实测与真实订单提交，必须分别描述，不能相互替代。
- 不提交 `data/`、`logs/`、浏览器用户目录、数据库、会话令牌或依赖目录。
- 金额、价格、数量使用十进制字符串与 Decimal，避免浮点金额计算。
- 重要需求或阶段进度变化后同步更新交接文档，让其他设备能继续工作。

这些文档描述项目当前约定；用户后续明确指示优先。
